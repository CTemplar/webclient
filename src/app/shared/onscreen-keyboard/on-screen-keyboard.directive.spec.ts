import { OnScreenKeyboardDirective } from './on-screen-keyboard.directive';

describe('OnScreenKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new OnScreenKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
